import React from 'react';

const Modal = ({ title, subtitle, content, actions = [] }): JSX.Element => {
    return (
        <div>
            {title}
            <div>teste 2</div>
        </div>
    );
};

export default Modal;
